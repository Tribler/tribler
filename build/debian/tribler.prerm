#!/bin/sh

if which pip3 >/dev/null 2>&1; then
    # Remove pony orm from pip
    pip3 uninstall -y pony
    # Remove lz4 from pip
    pip3 uninstall -y lz4
    # Remove pyqtgraph
    pip3 uninstall -y pyqtgraph
    # Remove wheel the last
    pip3 uninstall -y wheel
fi
